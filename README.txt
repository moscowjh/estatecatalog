Catalog project underway as 'estatecatalog'

Flask routing - URLs for pages in web app complete.

Database is set-up as 'estatecatalog' using postgresql:
- run `database_setup.py` to set up database using sqlalchemy;
- from psql CLI, run `estatecatalog.sql` to populate database.

Next steps is to set up all templates and forms.
